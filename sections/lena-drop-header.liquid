{%- comment -%}
  New Arrivals bar — a heading plus a linked count, nothing more.

  The section renders only while the linked collection holds products, and is
  absent entirely otherwise. There is deliberately no empty-state copy: an
  "arrivals" heading over nothing is the dormant framing this replaced.

  Visibility is the collection's product count and nothing else — no date
  window, no staleness read, no cap. The app owns what "new" means and how long
  it lasts, via a single tag; the theme only reflects the result.
{%- endcomment -%}

{%- liquid
  comment
    Lena: a collection picker, not a typed handle.

    This setting used to be type: "text" and was looked up with
    collections[section.settings.collection_handle]. A typo, a renamed collection, or a
    pasted URL instead of a handle all produced a blank collection, na_count stayed 0, and
    the section hid itself - the same visible outcome as "the collection is legitimately
    empty", with nothing to tell the two apart. R1 was this exact failure in the Featured
    Piece section, reached by a different route.

    The setting id is deliberately unchanged. A collection setting stores its handle as a
    string, which is what was already saved, so the existing value survives the change;
    renaming the id would have discarded it.
  endcomment
  assign na_collection = section.settings.collection_handle
  comment
    all_products_count is the collection total. The similarly named products_count
    reflects the current tag-filtered view, which is not what visibility should
    hinge on.
  endcomment
  assign na_count = 0
  if na_collection != blank
    assign na_count = na_collection.all_products_count
  endif
-%}

{%- if na_count > 0 -%}
  {%- style -%}
    .section-{{ section.id }}-padding {
      padding-top: {{ section.settings.padding_top | times: 0.75 | round: 0 }}px;
      padding-bottom: {{ section.settings.padding_bottom | times: 0.75 | round: 0 }}px;
    }
    @media screen and (min-width: 750px) {
      .section-{{ section.id }}-padding {
        padding-top: {{ section.settings.padding_top }}px;
        padding-bottom: {{ section.settings.padding_bottom }}px;
      }
    }
  {%- endstyle -%}

  <div class="color-{{ section.settings.color_scheme }} gradient section-{{ section.id }}-padding">
    <div class="lena-drop-bar">
      <div>
        <div class="lena-section-eye"><span class="dm filled sm"></span> {{ section.settings.eyebrow }}</div>
        <h2 class="lena-section-h">{{ section.settings.heading }}</h2>
      </div>
      <a class="lena-view-all" href="{{ na_collection.url }}">
        {{ na_count }} new {% if na_count == 1 %}piece{% else %}pieces{% endif %} &rarr;
      </a>
    </div>
  </div>
{%- endif -%}

{% schema %}
{
  "name": "New Arrivals",
  "tag": "section",
  "class": "section",
  "disabled_on": {
    "groups": ["header", "footer"]
  },
  "settings": [
    {
      "type": "text",
      "id": "eyebrow",
      "label": "Eyebrow",
      "default": "Fresh From Vietnam"
    },
    {
      "type": "inline_richtext",
      "id": "heading",
      "label": "Heading",
      "default": "New <strong>Arrivals</strong>"
    },
    {
      "type": "collection",
      "id": "collection_handle",
      "label": "Collection",
      "info": "The whole section is hidden while this collection holds no products. No default is possible on a collection setting, so pick one here."
    },
    {
      "type": "color_scheme",
      "id": "color_scheme",
      "label": "t:sections.all.colors.label",
      "default": "scheme-2"
    },
    {
      "type": "range",
      "id": "padding_top",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_top",
      "default": 52
    },
    {
      "type": "range",
      "id": "padding_bottom",
      "min": 0,
      "max": 100,
      "step": 4,
      "unit": "px",
      "label": "t:sections.all.padding.padding_bottom",
      "default": 0
    }
  ],
  "presets": [
    {
      "name": "New Arrivals"
    }
  ]
}
{% endschema %}
